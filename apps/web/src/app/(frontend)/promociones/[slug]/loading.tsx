import { PlantillaDeArticuloCargando } from '@/components/templates'

/** Sin hero: la ficha de una promocion arranca directo en la columna. */
export default function PromocionCargando() {
  return <PlantillaDeArticuloCargando />
}
