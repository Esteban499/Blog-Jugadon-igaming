import { PlantillaDeMapaCargando } from '@/components/templates'

/** Dos filas de chips: la de tipo y la de provincias. */
export default function PuntosDeVentaCargando() {
  return <PlantillaDeMapaCargando filasDeFiltros={2} />
}
